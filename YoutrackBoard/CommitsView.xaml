<UserControl x:Class="YoutrackBoard.CommitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Style="{DynamicResource PersonDetailsHeader}" Visibility="{Binding ShowHeader}">Commits</TextBlock>
        <ItemsControl Grid.Row="1" Name="Commits" Style="{DynamicResource PersonDetailsPanel}">
        </ItemsControl>
    </Grid>
</UserControl>
