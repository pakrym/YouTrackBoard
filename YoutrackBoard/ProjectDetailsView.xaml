<UserControl x:Class="YoutrackBoard.ProjectDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Name="Name" FontSize="{StaticResource HeaderFontSize}" Grid.Row="0"/>
        <TabControl Grid.Row="1" ItemsSource="{Binding PersonDetails}">
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <StackPanel>
                        <ContentControl cal:Bind.Model="{Binding}" Height="300" Width="400"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ContentTemplate>
            <TabControl.ItemTemplate>
                <DataTemplate>
                     <TextBlock Text="{Binding Person.Name}"></TextBlock>
                </DataTemplate>    
            </TabControl.ItemTemplate>
        </TabControl>
        
    </Grid>
</UserControl>
